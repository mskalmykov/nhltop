apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.kubernetes.io/azure-dns-label-name: msk-epam-diploma
  labels:
    app: nhltop
  name: nhltop
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: nhltop
  sessionAffinity: None
  type: LoadBalancer
